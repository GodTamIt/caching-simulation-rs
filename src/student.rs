use definitions::{AccessType, Stats};

pub fn init(c1: u32, c2: u32, b: u32, s: u32) {
}

pub fn cache_access(access_type: AccessType, address: u64, stats: &mut Stats) {
}

pub fn finish(stats: &mut Stats) {
}